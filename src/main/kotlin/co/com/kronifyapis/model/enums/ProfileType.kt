/**
 * Enum que define el tipo de perfil de un usuario.
 * CLIENT: usuario que agenda citas como cliente
 * BUSINESS: usuario dueño de un negocio (puede tener empleados o ser empleado)
 */
package co.com.kronifyapis.model.enums

enum class ProfileType {
    CLIENT,
    BUSINESS
}
